function soma(n1, n2) {
    const resultado = n1 + n2;
    return resultado; // ao encontrar o return nada mais será execultado
}

/*
function saudacao(nome /parâmetro/) {
    return (`Bom dia! ${nome}`); // fazer a função retornar um valor
}
saudacao('Jorge');
saudacao('Maria');
saudacao('Luiz');

// a função não retorna valor nenhum
const variavel = saudacao('Jorge');
console.log(variavel);

// fazer a função retornar um valor utilizando "return"
// ao encontrar o return nada mais será execultado
function saudacao(nome) {
    return console.log(`Bom dia! ${nome}`);
}

function soma(n1, n2) {
    const resultado = n1 + n2;
    console.log('Olá mundo!')
    return resultado; // ao encontrar o return nada mais será execultado
}

const resultado = soma(2, 2);
console.log(resultado);

// utilizando uma função
function soma(n1, n2) {
    const resultado = n1 + n2;
    return resultado;
}

console.log(soma(2, 2));
console.log(soma(3, 1));
console.log(soma(5, 10));

// função anônima
const raiz = function(n) {
    return n ** 0.5;
}; // precisa de ponto e virgula

console.log(raiz(9));
console.log(raiz(16));
console.log(raiz(25));

// arrow function - simplificação, substituindo function por =>, retirando parenteses, pontos e virgulas e chaves
const raiz = n => n ** 0.5

console.log(raiz(9));
console.log(raiz(16));
console.log(raiz(25));
*/