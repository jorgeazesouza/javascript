//indice -       0       1        2
const alunos = ['Luiz', 'Maria', 'João'];

/*
// mostra os índices do array
console.log(alunos[0]);
console.log(alunos[1]);
console.log(alunos[2]);

// alterar valor do array
alunos[0] = 'Eduardo';
alunos[3] = 'Luiza';

// adicionar elementos no final do array
alunos[alunos.length] = 'Luiza';
alunos[alunos.length] = 'Fábio';
alunos[alunos.length] = 'Luana';

alunos.push('Luiza');
alunos.push('Fábio');

// adiciona elementos no começo do array
alunos.unshift('Luiza');
alunos.unshift('Fábio');

// remove elementos do final do array
alunos.pop();
const removido = alunos.pop(); // armazena em uma variável o valor removido

// remove elementos no começo do array
alunos.shift();
const removido = alunos.shift(); // armazena em uma variável o valor removido

// remover um índice de um array
delete alunos[1]; // deixa o array undefined no valor removido

// fatiamento
alunos.push('Luiza');
alunos.push('Eduardo');

console.log(alunos.slice(0, -2));

// Verificar o tipo
console.log(typeof alunos); // Object
console.log(alunos instanceof Array); // true

*/