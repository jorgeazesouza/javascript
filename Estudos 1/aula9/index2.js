// Tipos Primitivos de dados

// Strings - Texto
const nome = 'Jorge';
const nome1 = "Jorge";
const nome2 = `Jorge`;
console.log(`Meu nome é ${nome}`); // Tamplate string

// Number - Numero
const num = 10;
const num1 = 10.52; // Numero com ponto flutuante

// Undefined - valor indefinido (não aponta para local nenhum na memória)
let nomeAluno;

// Null - valor nulo (também não aponta para local nenhum na memória)
const sobrenome = null;

// Boolean - True or False (valor lógico)
const alunoAprovado = true;
const alunoReprovado = false;

// verificar o tipo de dado utilizando o typeof
console.log(typeof nome, nome);
console.log(typeof num, num);
console.log(typeof nomeAluno, nomeAluno);
console.log(typeof sobrenome, sobrenome);
console.log(typeof alunoAprovado, alunoAprovado);


