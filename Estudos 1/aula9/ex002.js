// Crie duas variáveis com números inteiros. Some os dois e exiba o resultado no console. 
// Depois crie um número de ponto flutuante e multiplique pelo resultado da soma.
const num1 = 10;
const num2 = 15;
const num3 = 22.48;
const soma = num1 + num2;
const mult = soma * num3;

console.log(`A soma entre o ${num1} e o ${num2} é igual à ${soma}.`);
console.log(`O resultado da soma multiplicado por ${num3} é igual à ${mult}.`);
