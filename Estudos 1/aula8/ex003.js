// Crie uma constante produto com o nome de um produto (string).
// Depois, crie uma variável quantidade com o número de unidades em estoque.
// Aumente a quantidade e exiba no console:
const produto = 'Mouse Gamer';
let quantidade = 100;
quantidade = quantidade + 50;

console.log(`Temos ${quantidade} ${produto} em estoque.`);